// Tokens now live in the shared @kari/mobile-core package (one source of truth
// across rider + driver). Re-exported here so `@/theme/tokens` imports stay stable.
export { colors, fonts } from '@kari/mobile-core';
